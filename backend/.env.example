# ── Vibe EDA 환경변수 ─────────────────────────────────────────────────────────
# 실제 값은 .env에 저장 (gitignored)

# Gemini API (Vibe Chat)
GEMINI_API_KEY=your-gemini-api-key

# Claude API (Agent Mode)
ANTHROPIC_API_KEY=your-anthropic-api-key

# 모델 설정 (기본값 사용 권장)
DEFAULT_VIBE_MODEL=gemini-2.5-flash
DEFAULT_AGENT_MODEL=claude-opus-4-7

# Plotly→PNG 렌더(kaleido / Chromium 번들) on/off — 기본 on
# 끄면 차트 UI 표시는 정상이지만 LLM 이 차트를 이미지로 못 봄(메타텍스트만 인지).
# 셀 실행마다 발생하는 chromium spawn 비용을 줄이고 싶으면 0 으로 끄세요.
# VIBE_ENABLE_KALEIDO=1

# 노트북 커널 namespace LRU 한도 (기본 20)
# VIBE_KERNEL_NS_MAX=20

# CORS (로컬 개발) — 프론트 포트가 9700
ALLOWED_ORIGINS=["http://localhost:9700","http://localhost:4173"]

# Snowflake (SQL 셀 실행 시 필요)
# SNOWFLAKE_ACCOUNT=your-account.region
# SNOWFLAKE_USER=your-username
# SNOWFLAKE_PASSWORD=your-password
# SNOWFLAKE_DATABASE=your-database
# SNOWFLAKE_WAREHOUSE=your-warehouse
# SNOWFLAKE_SCHEMA=PUBLIC
